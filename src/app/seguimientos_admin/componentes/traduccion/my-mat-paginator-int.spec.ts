import { MyMatPaginatorInt } from './my-mat-paginator-int';

describe('MyMatPaginatorInt', () => {
  it('should create an instance', () => {
    expect(new MyMatPaginatorInt()).toBeTruthy();
  });
});
